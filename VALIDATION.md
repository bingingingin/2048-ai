# 验证记录

验证日期：2026-09-11。环境：Windows，Python 3.11，MinGW-w64 g++ 13.1.0，`-O3 -std=c++17`。

引擎源码 `engine/solver.cpp` 的 SHA-256：

```text
b4bf753ae1253b4d2c436d1cec4651f714a50d7bd674637bffffefdc0aee9dd0
```

## 规则、引擎和自动控制

`python -m unittest discover -s tests -v`：**12 项通过**。

- 穷举等级 0–16 的四格行布局，共 **17⁴ = 83,521** 种，与独立 Python 规则实现逐一对照。
- 1,500 个随机整盘、四个方向，共 **6,000** 次对照，包含高于 65536 的方块，并检查移动前后总和守恒。
- 验证连续相等方块单次只合并一次、合并计分、空格移动和终局判断。
- 验证四个方向的高位合并，包括 `32768 + 32768 = 65536`。
- 使用构造棋盘验证：可直接合成目标时立即选择该动作；控制器在出现 65536 后停止，不再自动开新局。
- 验证超时仍返回合法动作，过期搜索结果不会在暂停后执行。
- 验证失败重试、AI 单步、暂停续跑、存档恢复、随机数状态延续、无效移动不消耗随机数。
- 验证逐步重放时检查行动、随机落子、分数、步数、最终棋盘；损坏的结果会被拒绝。

**构造棋盘测试只证明规则和停止逻辑，不能作为从空盘达到 65536 的对局成绩。**

## 完整随机对局

最终版本批次命令：

```powershell
python autoplay.py --games 3 --seed 20260911 --budget-ms 10 --depth 8 --output runs/final-validation
```

采用标准规则：空格均匀抽取，生成 2 的概率为 90%，生成 4 的概率为 10%。没有修改概率、撤回坏落子或读取未来随机数。

**3 局全部完成**，结果如下；三份完整记录均已逐步重放核验。原始结果位于 `runs/final-validation/summary.json`，带引擎指纹的摘要副本位于 `validation/summary.json`。

公开仓库保留 `validation/summary.json` 作为本次验证快照；`runs/` 中的完整对局记录与实时存档仅保存在本地，不随 Git 上传。克隆仓库后可运行上述命令生成自己的记录。

| 随机种子 | 最大方块 | 分数 | 步数 | 耗时 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 20260911 | 16384 | 387304 | 14904 | 152.86 s | 无合法移动 |
| 20260912 | 16384 | 367020 | 13961 | 144.28 s | 无合法移动 |
| 20260913 | 8192 | 134568 | 5687 | 58.42 s | 无合法移动 |

本批次合计 **34,552 步**，最大方块为 **16384**，没有达到 65536。此处使用 10 ms 的测试预算；网页的深思模式为 500 ms，尚未完成该模式下的成功率评估。

这些是少量指定种子的测试，不构成可靠的总体成功率估计。时间预算会受 CPU 负载影响，即使种子相同，重新求解的轨迹也可能不同；已保存的行动和落子可以精确重放。

**目前尚未验证从空盘实际达到 65536，不承诺每局或持续重试必然成功。**

## 浏览器与交付检查

- 本地页面可加载，引擎连接正常；AI 单步、自动运行和暂停已通过实际浏览器交互验证。
- 服务重启后，棋盘、分数和步数与上次存档一致，并保持暂停；点击开始后继续运行。
- 深思模式设置正常；运行在 Python 后台线程，网页轮询状态，计算不会阻塞棋盘界面。
- 桌面与 390 px 宽布局经过浏览器检查，手机宽度下未出现横向溢出；浏览器未记录页面脚本错误。
- `node --check web/app.js` 和 Python 源文件编译检查通过。
- 已核对上游 MIT 许可并保留版权声明。参考项目临时克隆已删除，构建产物与运行记录分别保留在 `build/` 和 `runs/`。
